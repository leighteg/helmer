# helmer

The `helmer` crate is the actual runtime. It is pulled in as a dependency by most crates in this repo

`helmer` is responsible for the management/scheduling of tasks, workers, thread instances, extensions, etc..